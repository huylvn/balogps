PORT=3000
HTTPS_PORT=3443
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/balogps
JWT_SECRET=your-secret-key-change-in-production
NODE_ENV=development

# HTTPS Configuration
# Run 'npm run generate-cert' to create SSL certificate for development
# For production, use real SSL certificate (Let's Encrypt)
